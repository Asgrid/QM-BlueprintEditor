# QM-BlueprintEditor
A web form-based way to modify Quest Master blueprints.

## Usage
Copy a selection inside the game and the paste the text into the page. Its properties, tiles and entities will be displayed in editable fields. Changing the value on any of these will update the blueprint text automatically, so once you are done editing simply copy the new text and paste it back in the game.